describe('DeliveryTests', () => {
  test.each([[-1], [0], [1]])(
    'detects an invalid delivery date',
    (daysFromNow) => {
      // your test here
    },
  );

  test('the soonest delivery date is two days from now', () => {
    // your test here
  });
});
